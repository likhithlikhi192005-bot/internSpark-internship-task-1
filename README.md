# internSpark-internship-task-1
This project is a Python automation script that performs file management tasks such as renaming, sorting, moving, and deleting unwanted files. It uses the OS module, exception handling for error management, logging to track operations, and user input for flexible execution. The project includes GitHub source code and sample input/output.
